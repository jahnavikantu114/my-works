create a new repo and write index html file in it
goto aws acc
create a IAM and setup credentials
goto repo and select settings
left side environments--secrets and variables and go to actions and setup accesskey and secretkey---new repo secret
name it as access key and keep access key
again new and name it as secret key and give key
goto s3 bucket in aws
create a bucket name it and untick block all... versioning--enable
 
new repo again
name it as aws_s3_bucket
inside secret copy and paste bucket name
 
goto actions
workflow and keep this code
 
 
name: Upload Website
 
on:
  push:
    branches:
    - main
 
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v1
 
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1
 
    - name: Deploy static site to S3 bucket
      run: aws s3 sync ./ s3://BUCKET_NAME --delete   (change the bucket name here)....
 
 
 
goto s3 bucket and permissions bucket ownered ACLs enabled
properties enable static web page and save
goto actions it will build
goto bucket and select all objects and actions make public
